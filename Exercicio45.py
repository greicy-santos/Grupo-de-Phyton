numeros = [valor**2 for valor in range(1,11)]

print(numeros)

#A lista de Compreensões faz tudo em uma única linha...o resultado é o mesmo porém, ao invés de declarar uma lista vazia, depois fazer o for
#declarar uma variavel para fazer o calculo e ir adicionando os elementos na lista que são resultados do cálculo a lista.
